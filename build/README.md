# Build Resources

Place distributable app icons here before shipping final binaries.

Recommended files:

- `icon.ico` for Windows installer/exe
- `icon.icns` for macOS builds
- `icon.png` (512x512+) for Linux packages

If these files are missing, Electron Builder may use defaults.
